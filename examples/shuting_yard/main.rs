#![allow(unused)]

extern crate logic_tracer;

use logic_tracer::{
    parser::Parser,
    lexer::Lexer,

    operators::Operator,
    operators::LogicOp,
    operators::LogicOp::*,
};



fn main() {
    println!("Shuting Yard example (6)");
    panic!("Imlpement the Shuting Yard example (6)")
}
