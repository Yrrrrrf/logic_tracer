#![allow(unused)]

extern crate logic_tracer;

use logic_tracer::{
    parser::Parser,
    lexer::Lexer,

    operators::Operator,
    operators::LogicOp,
    operators::LogicOp::*,
};


/// Run:
/// ```bash
/// cargo run --example logic_op
/// ```
fn main() {
    println!("Logic Op example (7)");
    panic!("Imlpement the Logic Op example (7)")
}


